EF_APP_PATH=~/your/path/to/e-footprint-interface
DJANGO_DOCKER=False
# DATABASE_URL is only needed for Option 2 (hybrid local with Docker PostgreSQL)
# For Option 1 (full local with SQLite), leave DATABASE_URL commented out
# DATABASE_URL=postgresql://root:kakoukakou@localhost:5432/efootprint
MAX_PAYLOAD_SIZE_MB=30
# REDIS_URL is only needed when running with Redis (Docker/production)
# REDIS_URL=redis://redis:6379/0
