# Tests — e2e

End-to-end test patterns (Playwright fixtures, page object model, HTMX click helpers) are documented in `../../specs/testing.md` under "E2E tests (`tests/e2e/`)".

This pointer file exists so agents working from inside `tests/e2e/` notice the guide.
