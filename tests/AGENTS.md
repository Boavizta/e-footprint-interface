# Tests — e-footprint-interface

The canonical testing guide is `../specs/testing.md`. Read it before writing or editing tests.

This pointer file exists so agents working from inside `tests/` notice the guide.
