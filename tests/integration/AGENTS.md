# Tests — integration

Integration test patterns (Clean Architecture layer tests run without Django via `InMemorySystemRepository`) are documented in `../../specs/testing.md` under "Integration tests (`tests/integration/`)".

This pointer file exists so agents working from inside `tests/integration/` notice the guide.
