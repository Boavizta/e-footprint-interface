# Current-version browser benchmark — 2026-09-15

The reviewed comparison uses ten additional successful local warm-worker runs (`15:24:46Z` through `15:26:14Z`)
and the single successful production run `usage-20260915T151430Z-d8fe3ebe`. The local Django process remained running
for the complete campaign. Every browser execution used a fresh Playwright context.

| Scope | Local median (Q25–Q75) | Production | Production / local median |
|---|---:|---:|---:|
| All measured actions | 4,263 ms (4,235–4,293) | 16,303 ms | 3.82× |
| S1 Explore an example | 381 ms (370–406) | 2,610 ms | 6.85× |
| S2 Build and refine | 808 ms (779–821) | 4,149 ms | 5.13× |
| S3 Compare an alternative | 605 ms (584–616) | 2,344 ms | 3.87× |
| S4 Audit and reuse | 2,469 ms (2,446–2,483) | 7,200 ms | 2.92× |

These durations are sums of the explicitly measured browser actions, not server CPU times. Production has one
successful observation, so its ratios are calibration factors rather than estimates of production variability.

Two earlier successful local validation runs remain in `local/` but are excluded from this ten-run campaign. The
production folder also retains two failed preliminary attempts as required by the measurement method: the first used
a trailing-slash base URL that produced `//`, and the second exposed a reset/import HTMX race in the runner. Only the
successful run above is used in the comparison.
